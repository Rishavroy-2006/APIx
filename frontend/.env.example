# Copy this to .env.local for local dev, or set as Vercel env var for production.
# Replace the value below with your actual deployed Render backend URL.
VITE_API_BASE_URL=https://YOUR-RENDER-SERVICE.onrender.com
